import styled from 'styled-components';

export const Container = styled.div`
	margin: 1rem;
	margin-right: 2rem;

	h3 {
		color: var(--text);
		font-size: 0.9rem;
		font-weight: bold;
	}

	p {
		margin-top: 5px;
		font-weight: 600;
		font-size: 1.2rem;
	}
`;
